# require Logger
# webserver = {Bandit, plug: MyRouter, scheme: :http, port: 4000}
# {:ok, _} = Supervisor.start_link([webserver], strategy: :one_for_one)
# Logger.info("Plug now running on localhost:4000")
# Process.sleep(:infinity)
